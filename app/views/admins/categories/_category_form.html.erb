<%= bootstrap_form_for [:admins, @category] do |f| %>

  <%= render 'shared/errors', obj: f.object %>
  <%= f.text_field :name, required: true %>

  <%= f.submit "登録", class:"btn btn-primary" %>

<% end %>