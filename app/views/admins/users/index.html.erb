<% @page_title = "管理者/ユーザーページ" %>

<h2>ユーザー一覧ページ</h2>

<% @users.each do |user| %>
  <%= link_to user.last_name + user.first_name, admins_user_path(user.id) %>
  <!-- TODO: nameの書き方を見直す -->
  <%= user.created_at.to_date.strftime("%Y/%m/%d") %>
<% end %>

