<% @page_title = "検索結果ページ" %>

<h2>検索結果ページ</h2>

<% @search_lessons.each do |lesson| %>
  <%= link_to lesson_path(lesson.id) do %>
	<% if lesson.image? %>
    <%= image_tag lesson.image.url, width: 300, heith: 200 %>
  <% end %>
	<%= lesson.rank.name %>
	<%= lesson.name %>
	<%= lesson.display_time %>
  <% end %>
<% end %>

<% if @search_lessons.blank? %>
  <%= "該当するレッスンはありませんでした" %>
<% end %>